<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Wat de app mag, en niets meer.

      Hier stond eerder ook: de camera (voor een QR-lezer), de plaats van het toestel, opname van
      het scherm, en een dienst voor bediening op afstand. Die zijn er allemaal uit. Wat overblijft
      is wat je nodig hebt om bestanden op te halen en neer te zetten:

        · internet, en weten of er verbinding is;
        · een voorgronddienst met een melding — anders legt Android de sync na een paar minuten stil;
        · opnieuw starten na een herstart van de tablet;
        · een apk mogen installeren, voor de eigen bijwerking;
        · buiten de batterijbesparing mogen blijven — anders legt Android de dienst na een
          paar dagen stilstand alsnog stil, en dan staat de werf er 's morgens niet.

      Naar de map waarin geschreven wordt, wordt niet met een toestemming gevraagd maar met de
      mappenkiezer van Android: de machinist wijst er één aan en de app kan bij die ene map. Dat
      is nauwer dan "toegang tot alle bestanden" en het is voor hem ook duidelijker.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="MV3D"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Voor de eigen bijwerking: de gedownloade apk aan de installer van Android geven. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
